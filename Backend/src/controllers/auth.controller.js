export const signup = (req,rex) => {
    res.send("Signup route");
};

export const login = (req,rex) => {
    res.send("Signup route");
};

export const logout = (req,rex) => {
    res.send("Signup route");
};